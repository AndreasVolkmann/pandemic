package User
import City

public class User
    player p
    string name
    City currentLocation 
    unit hero

    construct(player p)
        this.p = p
        name = p.getName()
        currentLocation = cities.get("London") // TODO make random or the like
        Log.debug(currentLocation.name)
        hero = GetUnitsOfPlayerAll(p).next() // later on choose a hero
        hero.setPos(currentLocation.loc)