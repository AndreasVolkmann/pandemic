package System
import User
import LinkedList

let users = new LinkedList<User>()

public function start()
    let u = new User(players[0])
    users.add(u)

    users.forEach(u -> startTurn(u))
    

function startTurn(User u)
    Log.info("It is " + u.name + "'s turn")
    
